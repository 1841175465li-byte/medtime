# 药记 1.8.0 使用说明

药记是离线的用药记录与辅助提醒工具，无需账号。药品、规格、每次用量和频率由你按实际情况填写，应用不推荐用药或剂量。

## 安装与升级

安装 medtime-1.8.0.apk，最低支持 Android 8.0。已有药记可直接覆盖安装；本次沿用原包名和签名，升级保留本地药品、记录及已设置的提醒。建议升级前先导出备份，不要先卸载旧版。

首次安装的个人药品列表为空，不会自动加入药品或示例记录。旧版已经存在的药品、用量、频率和记录保留，不会推断历史规格、剂型或剂量。原有四个时段开关均保留；旧版没有睡前时段时，初始为关闭。

## 首次设置

1. 首页点 **添加我的药品**，搜索药品库，或点 **手动添加**。
2. 核对药名，可填写 **规格**、**剂型**和**默认每次用量**，点 **保存并设置用药安排**。规格表示产品标注信息，每次用量表示你实际使用的数量，两者分别保存。
3. 选择实际重复周期和时段，设置日期及提醒时间，点 **保存并检查提醒**。应用不会替你选择用药频率。
4. 点 **开启上述时段提醒**，在安卓系统中完成通知和准时闹钟授权，再试听和测试锁屏提醒。
5. 点 **完成设置，查看今日安排**。关闭页面也会保留此前已保存的药品和安排，可在药品卡片中继续修改。

没有固定频率时，选择 **不安排固定频率**，以后通过“补记用药”记录。已有 JSON 备份也可从欢迎页直接恢复。

## 安排、独立时间与疗程

进入 **药品 → 我的药品 → 设置安排**，或从首页 **设置安排**进入。

| 设置 | 含义 |
| --- | --- |
| 每天 | 每天在勾选的时段生成安排 |
| 每隔几天 | 填写 2–365 的整数，从开始日期按日历日计算；2 表示隔天 |
| 每周指定日期 | 在勾选的星期生成安排，可多选 |
| 不安排固定频率 | 不生成每日安排，仍可补记 |
| 早上／中午／晚上／睡前 | 四个可分别选择的时段 |
| 跟随时段默认时间 | 使用首页时段标题旁的默认闹钟时间 |
| 这款药使用独立时间 | 点击时间，弹出小时／分钟两列上下滑动选择器，不改变其他药品 |
| 开始日期 | 从这天起生效 |
| 结束日期 | 选填；结束当天仍生效，次日起停止安排和提醒，保留记录 |

点击独立时间会弹出两列选择器，分别上下滑动小时和分钟，中间高亮数字是当前值。点 **确定时间**回填到正在编辑的安排，再点 **保存安排**保存整份设置。点取消、关闭或安卓返回键会回到原安排，保留其他未保存的编辑。

例如，同为“早上”的两种药可分别设置 08:00 和 09:15。药品下方显示其实际安排时间，时段标题旁显示该时段默认时间。

**首页时段开关控制该时段内所有药品，包括使用独立时间的药品。** 修改默认时间不改变药品的独立时间。若只想停止一种药的提醒，请在该药“药品信息”中暂停安排。

## 提醒与锁屏试响

进入 **设置 → 用药提醒**，查看四个时段的默认时间与开关、通知／准时闹钟授权及系统闹钟音量。默认时间可使用小时／分钟双列滚轮调整。

开启开关不等于具备全部系统条件。通知或准时闹钟未授权、闹钟音量为 0、排程失败时，界面显示待处理状态。检测到省电、后台限制或勿扰模式时，会提示检查系统设置。不同手机的实际表现仍需测试。

- **立即试听（5 秒）**：检查当前铃声和音量，不增加记录。
- **1 分钟后锁屏试响**：安排独立测试，回到手机桌面并锁屏，等待响铃后再返回应用。
- 触发后显示计划和实际触发时间，请亲自选择 **已听到响铃** 或 **没有听到**。播放器启动不会自动被标记为实际听到。
- **取消这次测试**：取消仍在等待中的试响，不改变正式用药安排。

正常提醒最多响铃 1 分钟。安卓通知仅保留 **停止响铃** 和 **打开药记** 两个按钮。停止响铃不会新增或完成用药记录，打开药记可查看今日安排并按实际情况记录。

1.7.1 移除了“稍后 10 分钟”，升级后会清理旧版已排定的稍后提醒，不影响正常用药安排。

重启解锁、应用升级或时区变化后会尝试重新排程。系统“强行停止”应用后需重新打开药记。网页预览可保存安排，后台和锁屏提醒需使用安卓安装版。

## 药品信息、暂停与归档

个人药品卡片提供 **设置安排**、**设置用量**、**药品信息**。

- **规格／剂型**可在添加或药品信息中填写。相同名称的不同规格或剂型可以手动分别添加；名称、规格和剂型均相同的重复项会被阻止。
- **暂停安排**停止这一药品在首页的安排和提醒，保留频率、时间、用量和全部记录。恢复后按已保存的疗程及时段开关执行。
- **归档药品**移入 **已归档**列表，停止安排和提醒，历史记录仍可查看。
- **移回药品列表，保持暂停**将药品移回“我的药品”，仍保持暂停；需要启用时再点“恢复安排”。

暂停和归档调整应用中的安排，不代表建议开始或停止服药。修改当前药名、规格、剂型或默认用量不会改写旧记录。

## 记录、跳过与统计

实际用药后，点首页药品旁的 **记录**，保存当前时间和该药默认用量。刚刚记录过同一种药时会确认是否再次记录。没有记录的安排明确显示 **未记录**，应用不会据此判断漏服。

首页与药品列表会展示 **上次用药时间和当次用量**。用量未填写时会如实标注，不用当前默认用量补写旧记录。

**补记用药**可填写实际时间、时段、当次用量和备注。“不分时段”不会自动完成早、中、晚或睡前安排。当日已有未分时段记录时可 **关联记录**，不用重复添加。

本次没有用药时，可点 **本次跳过**并填写原因。跳过只对应这款药、这一天和这个时段，不计入用药次数，停止对应的待提醒。记录页单独显示跳过明细；点 **已跳过**可修改原因或撤销。跳过仅记录事实，不代表应用建议停药。

之后补记同药、同一天、同时段的实际用药，会清除对应跳过状态，保存实际用药记录。未分时段的补记不会清除任何具体时段的跳过。撤销跳过后恢复为未记录，只有尚未触发且时间未过的安排才会继续提醒。

记录页的 **近 7 天记录**分别汇总用药次数和跳过次数，空白日显示 0，点击日期可筛选明细。药品筛选也作用于这份概览；累计用药次数只统计用药记录，不包含跳过。

点记录旁的省略号可修改或删除；新增、修改、删除与跳过操作可在提示中撤销（约 8 秒）。设置变更和导入不提供撤销。每条用药记录计 **1 次**，不是药片数量。暂停、归档或疗程结束不会减少历史累计次数。

明细保留当时的药名、规格、剂型及当次用量。将用药记录改为另一种药时，改用所选药品的信息；只改时间或备注时保留原信息。

## 搜索与药品库

内置 90 项药品和营养补充品名称，支持中文、拼音、首字母和分类浏览，例如“二甲双胍”“er jia shuang gua”“ejsg”。个人列表也可按规格和剂型搜索。

库中名称仅帮助查找，不预设剂量或频率。没有找到的药品、或需区分的同名不同规格产品，可以手动添加。来源见 [药品库说明](./药品库说明.md)。

## 备份与恢复

进入 **设置 → 备份与恢复**，查看上次成功备份时间，并设置每 7／14／30 天提醒或关闭。提醒在打开应用时显示，不会自动备份或后台响铃。

### 保存备份

1. 点 **导出备份**，选择普通 JSON 或密码加密备份。
2. 加密备份需两次输入相同的 10–128 字符密码。请另外保存密码，忘记后无法恢复此文件；应用不会持久保存密码。加密保护导出文件，本机记录仍保存在应用私有存储。
3. 点 **生成并保存备份**，在 Android 系统文件选择器选择保存位置。完成写入与关闭文件后，才更新成功备份时间；取消或写入失败不会更新。
4. 网页预览无法确认文件是否落盘，需要在下载列表检查后点 **已确认文件保存成功**，成功时间会注明由你确认。

备份包括药品信息、暂停／归档状态、频率、独立时间、结束日期、默认用量、用药及跳过记录。普通文件可直接阅读，请妥善保管。当前系统无法使用加密功能时会显示错误，不会悄悄改为普通备份。

### 恢复与换机

1. 在新手机安装药记，在欢迎页或 **备份与恢复 → 从备份恢复**选择文件。
2. 如果是加密备份，输入密码后先本机解密。密码错误或文件损坏时不会改动已有记录。
3. 核对将新增的药品、用药与跳过记录数量，以及补全的信息，再点 **确认导入**。
4. 完成后核对记录，并到 **用药提醒**检查新手机的时段默认时间、开关、通知／准时闹钟权限和锁屏试响。
5. 确认新手机可用前，保留旧手机数据与备份。完整步骤可在 **换机恢复指南**随时查看。

导入采用合并：添加新药品和新记录，补全当前缺少的信息。本机已设置的信息、暂停／归档状态和相同 ID 的用药记录保留。跳过也按药品、日期和时段去重；已有实际用药的对应时段，以用药记录为准。重复导入不会重复增加记录。

1.8.0 使用 v5 数据结构，可读取 v1–v4 旧备份，旧记录不会被推断为跳过。**1.8.0 新备份需用 1.8.0 或兼容更新版本恢复。** 设备时段开关、全局默认提醒时间、系统授权、显示偏好、备份时间和试响结果不在药品 JSON 中，换机需重新设置。

数据仅存在本机应用存储。卸载、清除数据或设备损坏可能导致未备份记录丢失，导出文件需在保存位置单独管理。

## 字体、无障碍与维护入口

Android 安装版随系统字号变化。进入 **设置 → 字体与显示**可开启 **大字模式**，在系统字号基础上再放大 25%，设置会在本机保留。大字号下允许内容折行和纵向滚动；选时仍是小时／分钟两列。

主要操作区扩大到至少 48 CSS 像素。弹窗打开时隔离背景，键盘焦点留在当前弹窗，关闭后恢复原位置。时间列支持键盘上下键、Home 和 End；界面提供控件标签。TalkBack 的实际朗读与手势仍需手机验收。

进入 **设置 → 关于药记**查看 **版本更新说明、问题反馈、隐私政策、免责声明**。GitHub 反馈会在系统浏览器打开，需你登录并发送；应用不会自动附带用药记录。反馈可能公开，请不要附上备份、密码或身份信息。

独立文件：[更新记录](./CHANGELOG.md)、[隐私政策](./PRIVACY.md)、[公开发布准备](./发布准备.md)。

## 在你的手机上验收

以下项目尚未由本次开发环境完成真机验证。可先导出备份，用单独的“测试药品”安排检查，避免为测试添加实际用药记录。

| 操作 | 预期结果 |
| --- | --- |
| 授权通知／准时闹钟，调高音量后立即试听 | 听到约 5 秒铃声，记录数不变 |
| 安排 1 分钟后试响，回桌面并锁屏 | 手机提醒，回到应用亲自确认是否听到 |
| 在日常省电／勿扰状态下重复锁屏测试 | 根据实际结果调整系统设置，不把程序触发当作听到 |
| 为两种测试药设置同一时段的不同近未来时间 | 分别按独立时间提醒 |
| 展开正式提醒通知 | 只显示“停止响铃”和“打开药记”两个操作按钮 |
| 点击“停止响铃”／“打开药记” | 分别停止声音／回到今日安排，不自动增加记录 |
| 点击药品独立时间并分别滑动两列 | 弹出双列选择器，确认后回填时间；取消保留原值 |
| 设置已结束的疗程 | 首页不再列出，历史仍可查看 |
| 安排未来提醒后重启并解锁手机 | 设置恢复，后续时间正常触发 |
| 覆盖升级、导出及重新导入备份 | 原数据保留，不重复增加记录 |
| 本次跳过，再补记同药同日同时段用药 | 跳过不计入用药次数；实际用药替换对应跳过 |
| 撤销仍在未来的跳过安排 | 重新成为待提醒；已过时间或已触发的提醒不追补 |
| 普通／加密导出，分别取消、成功保存、模拟保存失败 | 只有写入成功才更新成功备份时间 |
| 用新手机恢复加密备份，先输错再输对密码 | 输错不改数据，输对后核对预览并合并 |
| 开启最大系统字号，再开大字模式 | 控件可读、可点，弹窗可滚动，时间仍可选择 |
| 开启 TalkBack 操作设置、记录、跳过和时间弹窗 | 名称可理解，焦点不进入弹窗背景，返回可继续原操作 |

关于药记内的 **免责声明**说明用途、提醒和数据限制；**赞助开发者**仍显示“赞助方式暂未开放”，没有收款功能。

本版完成数据规则、浏览器交互、原生排程规则和 APK 构建检查。浏览器的安卓接口模拟只验证界面与参数，不能证明真实设备的声音、振动、锁屏、后台或厂商省电行为。
