# 药记 1.6.0

一个简洁的安卓用药时间记录工具。离线药品库现有 **90 项、11 个类别**，包括原有的 70 个药名和新增的 20 项保健品/营养补充品。支持搜索、个人服药频率、每次用量、早中晚定时闹钟和累计用药次数。闹钟在首页直接开关，时间使用左侧小时、右侧分钟的双列滚轮；设置中可查看赞助开发者入口及免责声明。本版移除了“我的药品”卡片内的记录按钮，记录入口集中在首页及“补记用药”。

首次安装时「我的药品」预置 **米诺地尔、异维A酸软胶囊、非那雄胺** 三个名称，频率与用量均未设置；其他药品按需选择，不会自动加入每日安排。预置名称不构成用药推荐。

## 安装

1. 把本文件夹中的 `medtime-1.6.0.apk` 发送到安卓手机。
2. 在手机上打开 APK 安装；系统如提示，请为你打开文件的应用允许此次安装来源。
3. 打开桌面的“药记”。无需账号和网络。

安装包最低支持 Android 8.0，使用手机系统 WebView 显示界面。建议保持系统 WebView 更新。此版本为个人本地签名安装包，没有上传应用商店。

已安装旧版时，直接打开新版 APK 覆盖安装即可；本版沿用相同包名和签名，保留旧记录、已设置的频率及闹钟时间和开关。建议升级前导出一次备份，不要先卸载旧版。首次打开会自动兼容旧数据，旧记录仍保留原药名、时间和备注。

## 设置早中晚闹钟

1. 先为药品或补充品设置服用周期，并勾选早上、中午、晚上时段。
2. 首页每个时段右侧都有 **时间按钮和闹钟开关**。点开关立即开启或关闭该时段闹钟，无需进入设置页面或另点保存；其他时段不受影响。
3. 要改时间，点开关旁的时间（如 **08:00**），左侧上下滑动选小时、右侧选分钟，再点 **保存时间**。闹钟关闭时也能先改时间；保存时间不会改变首页开关。
4. 未获得通知或准时闹钟授权时，首页会明确提示等待授权。点 **查看设置**，分别允许通知和准时闹钟。返回药记后更新状态。
5. 时间页面仍有 **测试响铃（5 秒）** 和 **停止响铃**。实际提醒最多响铃 1 分钟，使用系统闹钟铃声和音量，并振动。通知栏也有停止按钮。

滚轮使用 24 小时制，小时为 00–23、分钟为 00–59；中间高亮数字是当前选择。也可点相邻数字微调。保存按钮就在时间选择下方；未保存直接关闭时，原时间不变。

早、中、晚的时间和开关相互独立。首次安装或从不含闹钟的旧版升级时默认都关闭；08:00、12:30、20:00 是待设置的初始时间，可以修改。开启且系统就绪时开关为绿色；等待授权或网页无法响铃时会提示尚未就绪。

闹钟仅在对应日期和时段有**尚未记录完成的安排**时提醒。每天、每隔几天、每周指定日期均按原有服药频率计算。当天无安排或本时段已全部记录时跳过；没有设置服药频率的药品不会触发定时闹钟。保存时今天的时间已过，则从下一次安排开始，不立即补响。设置弹层会显示已保存的下次提醒时间，或提示暂无待提醒安排。

未分时段的记录需关联到早中晚后，才算完成该时段。编辑、删除、撤销记录或修改频率后，闹钟安排会同步更新。停止响铃只关闭提醒，**不会增加服药次数或自动标记已服药**。

闹钟由安卓系统排程，可在退出应用界面或锁屏后触发。设备重启并解锁、改变时区/时间或升级应用后会重新排程。强制停止应用会使安卓停止其闹钟，需重新打开药记；关机时无法响铃。部分手机的后台限制和勿扰设置也会影响实际表现。建议在自己的手机上先做一次测试，并确认系统闹钟音量。

网页预览只能保存和查看闹钟设置，不能在后台响铃。闹钟时间保存在当前设备上；JSON 备份包含药品、频率、默认每次用量及记录（包括当次用量），换机恢复后请重新设置闹钟及系统授权。

## 设置每次用量

1. 打开底部 **药品 → 我的药品**，在对应药品下点 **设置用量**。
2. 填写数量，选择单位，然后点 **保存用量**。例如 `1粒`、`1 mL`、`0.5片`；数量支持小数。
3. 常用单位有粒、片、mL、滴、喷、袋、支、g、mg；选择 **自定义** 可填写其他单位。

每种药和补充品分别设置；没有设置时保持空白，应用不预填剂量。数量须大于 0，整数及小数各最多 6 位；单位不能空白。数量留空并保存可清除默认用量。

首页当天安排和“我的药品”显示默认每次用量。点“记录”时会把当前默认用量保存到该条记录；**之后改默认用量，不会改动旧记录**。旧版本的历史记录保持未记录用量，不自动补填。

补记用药会带入当前所选药品的默认用量，可按实际情况修改；编辑已有记录会显示该记录原来的用量。更改“本次用量”只影响这一条记录，留空表示未记录用量。历史明细会显示当次数量和单位，次数仍按记录条数计算：记录 `2粒` 也是 1 次。

应用只记录你填写的数量，不建议剂量，也不做单位换算。默认用量与历史记录用量都包含在新版备份中；含用量的备份请使用 1.4.0 或之后的兼容版本恢复。新版本可导入此前版本的备份。

## 搜索和添加药品

底部 **药品 → 药品库**，搜索药名或选择类别，然后点 **添加**。添加后切到 **我的药品**，卡片内提供 **设置频率、设置用量、改名** 三项操作。实际用药后，到首页对应时段点 **记录**，或通过 **补记用药** 填写时间；我的药品卡片不再重复放置记录按钮。

- 中文：例如 `二甲双胍`，也可输入部分药名。
- 拼音：例如 `erjiashuanggua` 或 `er jia shuang gua`。
- 首字母：例如 `ejsg`；不区分英文大小写。
- 支持中文别名查询，例如 `坦洛新` 对应 `坦索罗辛`。原来的三个药品也可搜索。
- 已添加的药品会显示“已添加”，避免重复选择。同一药品改名后，原有的药品库关联会保留。
- 未找到时点 **手动添加**，输入完整药名。自定义药品可按显示名称搜索；按内置通用名添加的药品支持拼音搜索。

分类包括血压与心血管、血糖、血脂、甲状腺与骨骼、呼吸与过敏、消化与肝脏、神经与精神、风湿与痛风、皮肤与毛发、泌尿与眼科。库中以通用名为主，需要分别记录普通片、缓释片等剂型或不同规格时，请手动添加完整名称；应用不会按相似名称自动合并。名称来源与分组说明见 `药品库说明.md`。

新增 **保健品与营养补充** 类别，收录复合维生素、B 族维生素、维生素 C/D3/E、鱼油、藻油 DHA、辅酶 Q10、叶黄素、钙/铁/锌/镁补充剂、益生菌、氨基葡萄糖和蛋白粉等 20 项。可搜索 `鱼油`、`yu you`、`Omega-3`、`VC`、`Q10`、`钙片` 等。添加后可使用相同的频率、闹钟和次数统计功能。分类用于查找名称，不表示产品疗效或适合长期使用。

药品库用于方便查找名称，分类仅用于浏览；请按医嘱选择药品并填写频率。应用不预设任何药物的剂量或疗程。

## 查看各药累计次数

底部 **记录** 顶部显示 **累计用药次数**，列出每种药品的累计次数、最近用药时间，未记录的药品显示 0 次。超过四种药时可点 **查看全部**。点某种药可筛选下方明细。

每保存一条用药记录计 **1 次**，不是药片数量。同一天早晚分别记录计 2 次；未分时段的记录也计入。累计次数依据全部已保存记录计算，不受下方日期筛选影响；下方另列出筛选结果的次数。

修改、删除、撤销和导入后，次数会重新计算。仅修改一条记录的用量不改变次数。修改某条记录的药品，会将这一次计到更改后的药品。仅修改药品名称会保留累计次数，历史明细仍显示记录当时的药名。

## 设置服药频率

首页点击 **设置频率** → 选择药品 → 选择重复周期 → 勾选早上、中午、晚上 → 设置开始日期 → **保存频率**。

- **每天**：每一天都会出现在勾选的时段；勾选一个、两个或三个时段，分别表示当天一次、两次或三次。
- **每隔几天**：填写 2–365 的整数。2 表示隔天，从开始日期计算，例如 1 日、3 日、5 日。每个安排用药的日期都按勾选时段显示。
- **每周指定日期**：勾选周一至周日中的日期，再选择用药时段。
- **不安排固定频率**：该药不出现在每日安排中，仍可补记用药。

每天的首页按 **早上、中午、晚上** 展示当天安排的药品。记录后只完成当前时段，并显示实际记录时间；当天未安排的时段会显示“无安排”。频率从开始日期当天生效，隔天和每周安排会自动判断今天是否需要显示。

新安装或从 1.0.0 升级时，三种药的频率均为“未设置”；从已有频率设置的版本升级，会保留原频率。新增药品也默认为未设置，请填写你实际采用的安排。应用不预设剂量或具体频率；系统闹钟需按上述步骤自行开启。

## 使用

- **记录当前时间**：在首页对应早、中、晚时段的药品旁点击“记录”。刚刚已记录过同一种药时，会先确认是否再次记录。
- **补记**：点击“补记用药”，选择药品、实际时间和时段，可填写备注。选择“不分时段”时仅保存实际时间，不自动勾选早中晚。
- **关联旧记录**：当天已有该药的未分时段记录时，首页显示“关联记录”。选择已有记录即可归类到对应时段，不会重复添加。需要新增时也可选择“仍然新增一条记录”。
- **查看和筛选**：底部“记录”上方显示各药累计次数，下方按日期列出明细，可筛选药品和具体日期。
- **修改和删除**：点记录右侧的省略号；删除前会确认。用药记录的新增、修改和删除仍可短暂撤销。闹钟、频率、每次用量、药品添加/改名及导入等设置操作不显示“撤销”。
- **添加或改名**：底部“药品”，也可从设置 → 药品管理进入。改名会保留历史记录当时的名称与累计次数。
- **备份和恢复**：设置 → 导出备份，JSON 同时包含药品、服药频率、默认每次用量和记录（包括当次用量）。导入时合并新记录，并恢复当前未设置的频率及默认用量；本地已有的频率、默认用量和相同 ID 记录会保留。可导入此前版本的备份；含用量的新版备份请使用 1.4.0 或之后的兼容版本恢复。

设置保存提示约 **1 秒** 后消失，提示文字不拦截点击；打开下一项设置会立即清除旧提示。保存失败会保留原值并明确提示错误。

记录时间按手机的日期、时间和时区显示。修改服药频率不会更改过去记录的时段或时间。

## 赞助开发者

设置 → **赞助开发者** 可进入支持页面。目前收款方式尚未确定，页面明确显示 **赞助方式暂未开放**，没有收款链接、二维码、金额选项或实际支付动作。确定方式后可在后续版本接入。

## 免责声明

设置 → **免责声明** 可随时查看。页面说明应用仅用于个人记录与辅助提醒、药品库名称不构成用药推荐、记录及设置需自行核对、设备和权限可能影响提醒，以及本地数据需妥善备份。

该说明不能替代医生、药师的专业指导，也不免除或限制依法不能免除、限制的责任。页面可上下滚动，底部点 **返回设置**，或点右上角关闭；无需勾选确认，不影响使用和已有数据。

## 数据

所有记录保存在应用本地；无需登录，不申请网络、相机、定位或广泛存储权限。1.3.0 增加了定时闹钟所需的通知、准时闹钟、重启恢复、短时前台音频播放、唤醒和振动权限；通知与准时闹钟按系统要求由你授权。导入导出仍使用系统文件选择器，只处理你选中的文件。

卸载应用、清除应用数据会移除记录，请定期导出备份。备份含药品与用药时间，建议保存在你自己管理的位置。相同签名的后续安装包可覆盖安装；请先备份再升级。

支持最多 100 种药品、20,000 条记录，实际总量也受系统本地存储配额限制。存储失败会明确提示，不会假装保存成功。损坏的原始数据不会被静默覆盖，可先导出留存。

## 源码和验证

- `web/`：可直接运行的离线前端源码，未附带测试用药记录。
- `android/`：Android 包装工程、构建脚本及开发说明。
- `tests/`：Node 回归测试，在本目录运行 `node --test tests/store.test.cjs tests/catalog-stats.test.cjs tests/reminders.test.cjs tests/dose.test.cjs`；`tests/java/` 为原生闹钟日历规则测试，运行方法见 Android 工程说明。
- `design/my-meds-v1.6.png`、`design/settings-v1.6.png`、`design/disclaimer-v1.6.png`：本版药品卡片、设置入口和免责声明预览。安装包不预置测试记录或启用闹钟。

本版通过 18 组手机/桌面浏览器检查（6 组本次界面检查、12 组既有流程回归），覆盖药品卡片三项操作、搜索和新增药品、首页记录与补记、免责声明阅读/滚动/关闭/返回、次数统计、编辑删除撤销、备份恢复及旧数据兼容。检查了 320、360、432 像素手机宽度和桌面宽度，未发现横向溢出、页面错误或控制台警告/错误。药品及闹钟数据逻辑未修改，相关 46 项 Node 测试已在 1.5 通过；原生闹钟日历逻辑已在 1.3 通过 30 项 Java 排程断言。本版原生代码经过编译，安装包校验资源、对齐与签名。

当前没有连接实体安卓设备，也没有安卓模拟器；尚未实际验证锁屏/后台响铃、声音与振动、系统授权、重启恢复、文件选择器及返回手势。安装后请先用“测试响铃”检查自己手机的表现。
